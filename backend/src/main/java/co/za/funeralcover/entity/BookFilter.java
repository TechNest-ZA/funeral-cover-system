package co.za.funeralcover.entity;

/** Filter chips on the admin "book" screen - operational views, not raw status. */
public enum BookFilter {
    everyone,
    pending,
    deceased,
    behind,
    new_this_month,
    from_funeral
}
